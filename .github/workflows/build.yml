name: Build Android APK

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Ensure Debug Keystore
      run: |
        if [ ! -f debug.keystore ]; then
          if [ -f debug.keystore.base64 ]; then
            base64 -d debug.keystore.base64 > debug.keystore 2>/dev/null || true
          fi
          if [ ! -f debug.keystore ] || [ ! -s debug.keystore ]; then
            keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          fi
        fi

    - name: Setup environment files
      run: |
        if [ -f .env.example ]; then
          cp .env.example .env
        fi
        if [ -n "${{ secrets.SUPABASE_URL }}" ]; then
          echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" > .env
        fi
        if [ -n "${{ secrets.SUPABASE_ANON_KEY }}" ]; then
          echo "SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}" >> .env
        fi
        if ! grep -q "SUPABASE_URL=" .env 2>/dev/null || grep -q "your-project.supabase.co" .env 2>/dev/null; then
          echo "SUPABASE_URL=https://rfhqbdwctqulvwwjcsgt.supabase.co" > .env
          echo "SUPABASE_ANON_KEY=sb_publishable_ZOtDEvWWBgdFJfaqe8fKfQ_-YMshS6S" >> .env
        fi

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK
      run: ./gradlew assembleDebug --no-daemon --stacktrace

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: LibDesk-Debug-APK
        path: app/build/outputs/apk/**/*.apk
        if-no-files-found: error

    - name: Create Release for Tags
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        files: app/build/outputs/apk/**/*.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

